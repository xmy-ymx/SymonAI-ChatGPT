import { Request } from './request';
import { CloudOptions } from '../types';
/**
 * Uni-app 环境请求类
 */
export declare class UniRequest extends Request {
    constructor(config: CloudOptions);
    /**
     * uni-app 环境请求方法
     * @override
     * @param data
     * @returns
     */
    request(url: string, data: any, _options?: any): Promise<any>;
}
