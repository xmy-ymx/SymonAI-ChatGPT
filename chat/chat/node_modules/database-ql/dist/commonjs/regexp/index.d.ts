/**
 * @deprecated This method was deprecated, use js native `RegExp` instead
 */
export declare class RegExp {
    $regex: string;
    $options: string;
    constructor({ regexp, options }: {
        regexp: string;
        options: string;
    });
    parse(): {
        $regex: string;
        $options: string;
    };
    get _internalType(): import("../utils/symbol").InternalSymbol;
}
/**
 * @deprecated This method was deprecated, use js native `RegExp` instead
 * @param param
 * @returns
 */
export declare function RegExpConstructor(param: {
    regexp: string;
    options: string;
}): RegExp;
