import { CloudOptions } from '../types';
import { Request } from './request';
/**
 * 微信小程序环境请求类
 */
export declare class WxmpRequest extends Request {
    constructor(config: CloudOptions);
    /**
     * 微信小程序环境请求方法
     * @override
     * @param data
     * @returns
     */
    request(url: string, data: any, _options?: any): Promise<unknown>;
}
