import { CloudOptions, RequestInterface } from '../types';
/**
 * 默认使用 axios 发送请求，可支持浏览器 和 Node.js 环境，如需支持其它平台，请派生子类并重写 `send()` 方法
 */
export declare class Request implements RequestInterface {
    protected options: CloudOptions;
    constructor(options: CloudOptions);
    /**
     * 发送 less-api 数据操作请求, 由 `Db` 中调用
     * @param action
     * @param data
     * @returns
     */
    send(action: string, data: object): Promise<any>;
    /**
     * 发出 HTTP 请求，主要于 `less-api` 数据请求和 `less-framework` 云函数调用时使用
     * 默认使用 axios 发送请求，可支持浏览器 和 Node.js 环境，如需支持其它平台，请派生子类并重写本方法
     * @param data
     * @returns
     */
    request(url: string, data: any): Promise<any>;
    /**
     * 获取必要的请求头
     * @param token
     * @returns
     */
    protected getHeaders(token: string, headers?: Object): Object;
}
